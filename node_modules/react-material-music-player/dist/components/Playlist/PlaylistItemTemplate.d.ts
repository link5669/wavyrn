export default PlaylistItemTemplate;
declare class PlaylistItemTemplate extends React.Component<any, any, any> {
    constructor(props: any);
    props: any;
    handleSelect(): void;
    render(): React.JSX.Element;
}
declare namespace PlaylistItemTemplate {
    namespace propTypes {
        const classes: PropTypes.Validator<object>;
    }
}
import React from "react";
import PropTypes from "prop-types";
